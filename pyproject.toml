[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agentic-cinema"
version = "0.1.0"
description = "Cutting Room Copilot: audience-analytics agent on Gemini Enterprise Agent Platform + ClickHouse"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
